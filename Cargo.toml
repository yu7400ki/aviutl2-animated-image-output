[workspace]
members = ["aviutl", "dialog", "apng", "gif", "webp"]
default-members = ["apng", "gif"]
resolver = "3"

[workspace.dependencies]
windows = { version = "0.61" }
widestring = { version = "1.2" }

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
